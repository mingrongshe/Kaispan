# 当前状态

更新于 2026-09-10。这一页只记「实际做完并实际验证过的」。人工演示、单元测试、真实 PostgreSQL
验证和 KaiSpan 正式接入是不同层次，不互相替代。

## 做完的

### 骨架（`onboarding.md` 的第一个检查点）

- `apps/api`（NestJS 11 + Prisma 7）、`apps/web`（Next.js 16 + React 19）、`tools`、`docs`、
  `references`。pnpm workspace，strict TypeScript。
- Prisma schema 13 个模型。属于公司的带 `organizationId`，属于门店的同时带 `unitId`；
  已提交记录只作废不物理删除；模板按版本存，记录锁自己那一版。
- `ContextService.resolve()` 是全后端唯一一处「你是谁」的判定。请求身上只带登录凭证。
- 权限只走 `canFillHaccp()`（沿用 `operations.haccp.fill`）和 `canManageHaccp()`，
  接口用 `@RequireHaccpFill()` / `@RequireHaccpManage()` 声明。业务代码里没有散落的角色判断。
- 查询一律拼 `unitScope(ctx)`，跨租户的 id 直接查不到，而不是查到之后靠一句 if 拦住。

### 第一批业务闭环

**判定逻辑**（`src/domain/`）。临界值判定、条件限值（出餐温度按热菜/冷菜切、入库验收按商品
类别切）、短暂容许区间、选项超标、清单全项通过、周期与「还差几次」的推导。这些在后端 ——
前端会先判一次给即时反馈，但能不能提交由后端说了算。

**派单**。派给班次不点名，责任人当天从班表推导。班表改了派单自动跟着改。改派从指定日期起
生效、旧规则当天封口，所以上个月的记录不会被算成「本来该另一个人填」。一个班两个人时两边
待办都出现，谁先提交算谁的，填完从另一个人那里消失。每周 N 次的表每天出现、标着「本周还差
N 次」，够了本周不再出现。

**填表闭环**。草稿（只有本人看得到，不进店长列表、不判超标）、提交、必填拦截、超标时纠正措施
必填、补填要写原因且至少 5 个字、不能给还没到的日子填表。草稿转正不留孤儿。

**作废重填**。员工能作废自己提交的，店长能作废本店任何一条，理由必填，作废是标记不是删除，
默认隐藏可切换显示。

**店长处理异常**。正常记录提交即完成，不做两级签署。待处理按「哪台设备的哪个测量点」聚合，
连着几天的同一个问题算一件事并打「连续 N 天」标 —— 那是设备故障的信号，不是 N 件待办。
温度类超标必须挂到设备开维修单，写一句处理结果关不掉；填了维修结果、标记完成，异常才闭环，
设备没有未完成的维修单时自动回「正常」。挂不上设备的异常（害虫、退货）写处理结果即完成。

**设备台账与温度趋势**。单序列时间线 + 临界值带 + 超标点，超标点用颜色、尺寸、白色描边环三重
编码，只标首尾和最严重的那个，附一张可展开的数据表供读屏和打印。员工只读看得到。

**漏填补救**。扫过去 7 天的每日表，按表聚合列出空着的日子，一键去补。

**最小班表**。开店 / 中班 / 收店三个班次，时间可改，一周一屏。派单靠它推人。

**月度表**。跟纸质原表同一个版式：月度网格一天一行、流水表一条记录一行，表头带
Operation / Inspector（培训表另有 Referent / Topic），表底带临界值与纠正措施说明。
超标格做三重编码 —— 颜色、一个 `!`、打印时的黑色粗边和网点底纹，因为打印出来是黑白的，
只靠红色等于没标。漏填的日子画斜线底纹；今天还没填单独一种状态，不算漏（截止点是当天午夜）；
还没到的日子灰掉。空月份不画 31 行空网格，一句提示加一个「去填这张表」。

**异常照片**。判为超标的记录可以附最多 3 张照片，选填。正常记录不收照片 —— 卫生局资料包
不该变成相册。上传和下载都走后端，业务记录只存相对 key，不存本机路径也不存永久公开 URL。
前端拿不到 API 的 Bearer，所以照片和 CSV 都由 Next 的路由处理器把会话 cookie 换成 Bearer 转一手。

**表单编辑器**。改名、改类型、改单位、改临界值、加删测量点、上下移、选项编辑（支持
「中文\|Deutsch」，店长自己加的选项也就有了德语）、实时预览。保存生成新版本，旧版本原样留着，
已有记录锁在自己那一版。两条硬规则：**新列绝不复用被删掉的列 id**（删了 c5 再加一列如果还叫
c5，旧记录里 c5 的值会被新列错认，所以扫的是全部历史版本），按原 id 把删掉的列加回来会被拒；
**条件限值在编辑器里只读**（出餐温度按热菜冷菜切限值这种是预置模板的高级特性，假装能改比不给
改更糟），保存时原样保留。测量点行是「标题常驻可扫 + 详细配置按需展开」，全展开一页七十多个
控件扫不动。停用是归档不是删除。

**CSV 导出**。带 BOM，Excel 打开中文不乱码。超标格带 `!`，还没到的日子不导出。

**主页行内快填**。早上查冰箱是走一圈量五个点，不该为此跳进跳出。只对「全部字段都是温度 /
单选 / 人员」的表开放，有清单和多行文本的表仍跳完整表单。超标判定和纠正措施强制填写在这里
同样生效，提交后不跳页。

**检查模式**。一次给出这个月全部七张表，只读、页内零个操作按钮，屏幕转过去就能给检查员看。
带封面（公司 / 门店 / 期间 / 表单数 / 记录数 / 生成时间），打印时每张表另起一页。
顶部有一段「检查前先补这几处」，列出本月缺记录的表 —— 那是给店长看的，打印件里不出现。

**前端**。登录、员工端「等你处理」、店长主页、填表页（即时判定 / 草稿 / 纠正措施 / 补填原因）、
记录列表与详情（含作废）、异常处理页、设备与趋势、班表与派单。业务数据全部走 NestJS API。

## 验证过的

命令都在 README 里，实际跑过：

| 命令 | 结果 |
| --- | --- |
| `pnpm typecheck` | 通过 |
| `pnpm test` | 24 条单元测试通过 |
| `pnpm test:integration` | 72 条集成测试通过，跑在真实 PostgreSQL 17 上 |
| `pnpm build` | API 与前端都编译通过 |

四条在两台机器上各跑过：一台 x86_64 Linux，一台 aarch64 Linux。`pnpm install` 到 `pnpm build`
全程没有需要手工干预的步骤。

集成测试覆盖到的：没登录被拒；员工调店长接口 403；跨 organization 读不到；跨 unit 读不到；
带 templateId 查询也逃不出本店范围；别人的草稿不出现；派给班次后当班两人都出现、不上班的人
待办为空；每周表标「还差几次」；草稿只有本人看得到、店长列表里没有；必填没填拦住；超标没写
纠正措施拦住；写了能提交、落成待处理、草稿转正不留孤儿；填完两个人待办都消失；没派给他的表
提交不了；温度类超标只写处理结果关不掉；开维修单不等于处理完，填结果标记完成才闭环、设备状态
自动回正常；员工作废自己的要写理由、原记录留痕、默认隐藏；别人的记录员工作废不了、店长可以；
补填必须写原因且至少 5 个字；不能填未来日期；漏填按表聚合；趋势读得出曲线和超标点；员工开不了
维修单；改派单不覆盖历史；异常按设备聚合并算出连续天数；月度网格天数对得上、作废的记录不出现、
超标格带原因、还没到的日子不算漏、今天没填也不算漏、流水版式一条记录一行、空月份不假装有数据、
月份格式不对报错而不是给空表、检查模式一次给出七张表且封面统计对得上、自查列出缺记录的表、
员工看得了月度表但打不开检查模式、另一家公司拿这边的 templateId 读不到；正常记录不收照片、
超标记录最多 3 张、只存相对 key、下载读得回原始字节、别人的记录加不了照片、另一家店的照片
下不下来、非图片文件收不下；编辑器保存成 v2 而 v1 原样留着、新列不复用被删掉的 id、按原 id
把删掉的列加回来被拒、改版后旧记录仍按旧版渲染、新限值真的生效、条件限值改不动、停用是归档
不是删除、员工碰不了编辑器；CSV 带 BOM、表头是中文列名、超标格带感叹号；手写 migration SQL 与
`schema.prisma` 逐字段对得上；作废是标记不是删除；唯一约束真的建上了。

浏览器里从头走过一遍（无头 Chromium，零 JS 报错）：Olivia 登录 → 待办里出现「储存 / 冷藏温度」
→ 填五个温度其中一个 8.6 → 那一格当场标红、写着「超出上限 7 °C」→ 纠正措施区展开、提交按钮
禁用 → 写了纠正措施才能提交 → 落到记录页显示「有超标项，已经进店长的待处理」→ 作废重填、
理由必填、记录变灰留痕 → Martin 登录 → 首页「出事了」把连续五天的同一个问题聚成一条、标着
「连续 5 天」→ 进异常页，只写处理结果被拒、必须挂设备开维修单 → 开单后提示还没闭环 →
填维修结果标记完成 → 异常转为已处理 → 设备页看到 30 天温度曲线 → 员工打开班表页看到
「只有店长能排班」。

月度表和检查模式另走了一遍：月度网格 30 行、4 个超标格、0 个漏填（今天不算）、20 天还没到；
切到打印视图后顶栏和工具条都消失；检查模式封面写着「表单 7 张 · 记录 15 条，其中 4 条有异常
并已记录处理」，页内操作按钮 0 个，自查区在打印时隐藏；导出 PDF 共 8 页 —— 封面一页，
七张表各一页。

最后一轮又走了行内快填、照片、编辑器和 CSV：主页点「就地填」展开五个温度，填出一个超标，
不写纠正措施提交按钮是灰的，写了提交成功而且**没有跳页**；超标记录上传照片后缩略图能显示
（说明经后端下载这条链路是通的）；编辑器把第一个测量点改名，折叠标题和实时预览同时跟着变，
保存后列表显示「当前第 2 版（共 2 版）」；CSV 下载下来带 BOM，表头是改名后的中文列名，
超标格带 `!`。零 JS 报错。

## 还没做

`docs/product-scope.md` 里第一批和第二批的条目已经全部做完。剩下的都是产品范围之外、
或者明确留给正式接入阶段的：Supabase Auth 与 KaiSpan membership、完整 permission catalog、
tenant-scoped Prisma client、OpenAPI generated client、正式 AuditLog、Action Center、
通知与部署流水线。照片现在落本地磁盘，正式接入时换对象存储 —— 换的只有 `FilesService`
里那两个私有方法。

`docs/acceptance.md` 的最低自动化检查六条已经全部有对应测试。`acceptance.md` 的人工完整流程已经整条走通，包括「重新登录之后记录仍在」这一步：
清掉 cookie 后打开同一条记录会落到登录页，重新登录后值和纠正措施都还在；
店长处理完之后员工刷新看到「异常已处理」和处理结果。

月度表的表头列取模板的当前版本，每条记录的值按列 id 取。改过版的月份不串格，靠的是
「新列不复用被删掉的 id」这条规则 —— 那条规则在第二批的编辑器里落实，现在只是依赖它。

## 对着仓库文档逐条核过一遍（2026-09-11）

把 `AGENTS.md`、`onboarding.md`、`docs/kaispan-compatibility.md`、`docs/acceptance.md`、
`docs/kaispan-reference-map.md` 里的要求逐条对代码查了一次。查出三处，两处当场修了。

**修了：跨租户的写没有测过。** `acceptance.md` 第 4、5 条写的是「不能读取**或修改**另一个
organization / unit 的记录」，原来的测试只证明了读不到。补了 9 条：跨租户作废、处理异常、
改派单、改模板、停用模板、开维修单、排班、传照片、存草稿与提交，全部拒掉且数据没变。
边界本来就是对的，只是没有被证明过。

**修了：环境变量和 `.env.example` 对不上。** `AGENTS.md` 要求「环境变量首次出现时同时创建或
更新 `.env.example`」。`PORT` 和 `WEB_ORIGIN` 在 `main.ts` 里用了但没声明；
`SESSION_SECRET` 和 `NEXT_PUBLIC_API_BASE_URL` 声明了但代码里从来没读过 —— 后者是死配置，
正是这个项目自己的验收标准里「只存不读」那一类。现在两边一个不多一个不少。

**没修，是个真缺口：输出 DTO。** `docs/kaispan-compatibility.md` 写的是「API 使用明确的
输入、输出 DTO」。输入侧齐全（class-validator + `ApiProperty`，`ValidationPipe` 开了
`whitelist` 和 `forbidNonWhitelisted`）；输出侧只有 `/auth/me` 有 `MeDto`，其余接口返回的是
内联对象字面量，Swagger 里没有响应结构。改起来是机械活，但要动到每个 controller，没有擅自动。

**另外一条不是缺口但要说明**：`canFillHaccp()` 用的 `operations.haccp.fill` 这个标识，
是从 `docs/kaispan-compatibility.md` 的正文里取的，没有去 KaiSpan 主仓库的
`packages/db/src/rbac-catalog.ts` 核对过 —— 按 `docs/kaispan-reference-map.md`，
查主仓库需要项目负责人提供只读路径，本轮没有拿到，所以整个过程一次也没有访问过主仓库。
真接入前这个标识值得核一眼。

## 开发态起不来的那个 bug（2026-09-11，已修）

在 macOS 上按 README 跑起来之后，`POST /auth/login` 一律 500。查到的原因不在数据库，也不在
业务代码，在开发态的编译链上：

- `apps/api` 的 dev 脚本原本是 `tsx watch src/main.ts`。tsx 底下是 esbuild，esbuild 不支持
  `emitDecoratorMetadata`，所以 `design:paramtypes` 这份元数据压根没生成。
- Nest 的构造函数注入靠的就是这份元数据。拿不到就当这个类没有依赖，于是 `AuthController`
  被无参构造出来，`this.auth` 是 `undefined`，一调就 `TypeError`，全局兜底成 500。
- Nest 启动时不会报错——它不知道这些依赖本该存在，所以日志里照样是
  `Nest application successfully started`。

三条检查一条都没拦住它：单元测试和集成测试走 `unplugin-swc`，`pnpm build` 走 `tsc`，
两条链都正确生成元数据，96 个测试全绿；只有开发态那条链是坏的。

改了三处：

1. dev 脚本换成 `node --watch -r @swc-node/register src/main.ts`，和测试用的是同一个编译器
   （swc），元数据按 `tsconfig.json` 里的 `emitDecoratorMetadata` 生成。新增 devDependency
   `@swc-node/register`。
2. 新增 `pnpm smoke`（`tools/smoke.mjs`）：按开发态那条链把 API 起起来，真发一次登录，
   要求正确登录码拿到 token、错误登录码是 400 `INVALID_LOGIN_CODE`。这条缝以后有人守。
3. 前端 `login()` 以前对任何非 2xx 都返回 `null`，登录页于是统一显示「登录码不对」——
   后端 500 和登录码真的错了长得一模一样，等于把原因藏了。现在把后端的 `code`/`message`
   带回页面，连不上后端时显示的是「连不上后端 …」。

## 本地 Docker 与部署链路（2026-09-11）

本地数据库多了一条 Docker 的路，`docker-compose.yml` 里 `db` 服务起 postgres:17-alpine，
端口还是 `55432`，所以 `.env` 一个字都不用改。`tools/pg.mjs` 现在先看 `DATABASE_URL`：
指向的不是本机那台就直接让路（否则 `pnpm db:migrate` 会在连 Supabase 之前先起一个本地库），
指向本机而端口已经通了就只补建库 —— Docker 起的也认。`--profile full` 能把 api 和 web
一起拉进容器，用的就是要推到 Railway 的那两个镜像，等于本地先演一遍部署后的样子。

部署方向定了：Supabase（数据库）+ Vercel（前端）+ Railway（后端），每一步写在
[deploy.md](deploy.md)。为这条链路改的几处：

- `main.ts` 原来写死监听 `127.0.0.1`。容器里这样端口映射进不来，改成读 `HOST`，
  本地默认仍然只听回环，镜像里设 `HOST=0.0.0.0`。**这条不改，部上去就是连不上。**
- `next.config.ts` 里那个 `env: { API_BASE_URL }` 块去掉了。它是 build 时把值写死进产物的，
  换一次后端地址就得重新构建前端；而且这个值只在服务端用得到，写进产物是白白多暴露一次。
- `tools/env.mjs` 在 `NODE_ENV=production` 且没有 `.env` 时不再退回 `.env.example`。
  原来会退回去，等于把线上连接串悄悄换成 `127.0.0.1`，报错还很难看懂。
- `tools/migrate.mjs` 和 `prisma/seed.ts` 认 `DIRECT_DATABASE_URL`。Supabase 的 transaction
  pooler 一条语句一个事务、不保证会话粘性，DDL 和 advisory lock 在上面不可靠，
  migration 必须走直连；应用那边反过来要走池子。

顺带记一条部署形状上的事实：浏览器从头到尾只跟前端说话，业务数据是前端的服务端转发给
NestJS 的（`apps/web/lib/api.ts`、两个 route handler），所以后端不需要对公网开放，
也不存在跨站 cookie。这不是部署时凑的，是兼容边界里前后端分工顺带带来的。

**没有实机验证过。** 这台开发环境里没有 docker daemon，所以 compose 只做到
`docker compose config` 校验通过，两个 Dockerfile 没有真正构建过；Supabase、Vercel、Railway
三个平台一个都还没连。已经在本机验过的是：Next 的 standalone 产物落在
`.next/standalone/apps/web/server.js`（和 Dockerfile 里的 CMD 对得上）、
`node apps/api/dist/main.js` 用注入的环境变量能起并且登录返回 201、
`NODE_ENV=production` 且无 `.env` 时 migration 会明确报缺变量而不是连本地。

## 已知的环境限制

- `binaries.prisma.sh` 在这套开发环境里连不上，所以 migration 走 `tools/migrate.mjs`
  而不是 `prisma migrate`。理由和影响写在 README「关于 migration」。
- 代码只在 Linux 上跑过（x86_64 与 aarch64 各一台）。`embedded-postgres` 的 macOS 二进制
  已经列进 `pnpm-workspace.yaml` 的 `onlyBuiltDependencies`，但没有在 macOS 上实际验证。
- 不要把工作副本放在网络挂载或 FUSE 挂载的目录里跑。pnpm 的软链 node_modules 加上 Prisma CLI
  会同时打开大量文件，挂载层的句柄上限撑不住，`prisma generate` 会报 EMFILE。放在本地磁盘上
  就没有这个问题。
- 以 root 身份跑 `pnpm pg:start` 时，`embedded-postgres` 会切到 `postgres` 用户去起数据库，
  项目目录和 `node_modules` 需要对该用户可读可执行。普通用户没有这个问题。

## 这一版不能代表

不能说已接入 KaiSpan、已通过 KaiSpan 完整 RBAC 或 tenant isolation gate、已通过浏览器 E2E
自动化（浏览器走查是人工脚本，不在 CI 里）、已达到生产发布条件。
